allure.results.directory=target/allure-results
allure.link.issue.pattern=https://your-jira-url/browse/{}
allure.link.tms.pattern=https://your-test-management-url/browse/{}

# First clean and run tests
#mvn clean test

# Generate the report
#mvn allure:report

# Serve the report (this should work now)
#allure serve target/allure-results
